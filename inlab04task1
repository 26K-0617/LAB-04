#include <stdio.h>
//task1: even/odd

int main(){
	int num;
	printf("Enter an integer: ");
	scanf("%d",&num);

    if (num % 2 == 0) {
        printf("Your number %d is even", num);
    }
    else {
        printf("Your number %d is odd", num);
    }
    
    return 0;
}
